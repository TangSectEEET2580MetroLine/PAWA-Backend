package rmit.edu.vn.hcmc_metro.ticket;

public enum TicketStatus {
    ACTIVE,    // Ticket is currently active and valid
    INACTIVE,  // Ticket is inactive and not usable
    EXPIRED    // Ticket has expired and is no longer valid
}
